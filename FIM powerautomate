@and(
  greaterOrEquals(
    item()?['receivedDateTime'],
    convertTimeZone(
      concat(formatDateTime(utcNow(),'yyyy-MM-dd'),'T07:00:00'),
      'India Standard Time',
      'UTC'
    )
  ),
  lessOrEquals(
    item()?['receivedDateTime'],
    convertTimeZone(
      concat(formatDateTime(utcNow(),'yyyy-MM-dd'),'T07:30:00'),
      'India Standard Time',
      'UTC'
    )
  )
)

condition:
expression length(body('Filter_array'))



length(body('Get_emails_(V3)')?['value'])
