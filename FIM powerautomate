@greaterOrEquals(
  item()?['receivedDateTime'],
  convertTimeZone(
    concat(formatDateTime(utcNow(),'yyyy-MM-dd'),'T07:00:00'),
    'India Standard Time',
    'UTC'
  )
)

condition:
length(body('Filter_array'))


length(body('Get_emails_(V3)')?['value'])
